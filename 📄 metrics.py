def kindness_score(response):
    # Placeholder logic for scoring
    return 0.8 if "thank" in response.lower() else 0.5

def coherence_score(context, response):
    return 0.9  # Assume coherent for prototype
